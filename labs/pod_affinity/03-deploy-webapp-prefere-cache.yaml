apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp-prefere-cache
  namespace: lab
spec:
  replicas: 6
  selector:
    matchLabels:
      app: webapp-prefere-cache
  template:
    metadata:
      labels:
        app: webapp-prefere-cache
    spec:
      affinity:
        podAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - cache
              topologyKey: kubernetes.io/hostname
      containers:
      - name: nginx
        image: nginx:alpine
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "100m"
            memory: "128Mi"
