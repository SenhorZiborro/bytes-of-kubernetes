apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp-mesma-regiao
  namespace: lab
spec:
  replicas: 4
  selector:
    matchLabels:
      app: webapp-mesma-regiao
  template:
    metadata:
      labels:
        app: webapp-mesma-regiao
    spec:
      affinity:
        podAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - cache
            topologyKey: regiao
      containers:
      - name: nginx
        image: nginx:alpine
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "100m"
            memory: "128Mi"
