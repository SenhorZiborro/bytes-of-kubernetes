apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp-ha
  namespace: lab
spec:
  replicas: 3
  selector:
    matchLabels:
      app: webapp-ha
  template:
    metadata:
      labels:
        app: webapp-ha
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - webapp-ha
            topologyKey: kubernetes.io/hostname
      containers:
      - name: nginx
        image: nginx:alpine
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "100m"
            memory: "128Mi"
