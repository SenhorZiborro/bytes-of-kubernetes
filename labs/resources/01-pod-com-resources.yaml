apiVersion: v1
kind: Pod
metadata:
  name: pod-com-resources
  namespace: lab
spec:
  containers:
  - name: nginx
    image: nginx:alpine
    resources:
      requests:
        memory: "64Mi"
        cpu: "100m"
      limits:
        memory: "128Mi"
        cpu: "250m"
