apiVersion: v1
kind: Pod
metadata:
  name: pod-guaranteed
  namespace: lab
spec:
  containers:
  - name: nginx
    image: nginx:alpine
    resources:
      requests:
        memory: "64Mi"
        cpu: "100m"
      limits:
        memory: "64Mi"
        cpu: "100m"
