apiVersion: v1
kind: Pod
metadata:
  name: pod-cpu-impossivel
  namespace: lab
spec:
  containers:
  - name: nginx
    image: nginx:alpine
    resources:
      requests:
        memory: "64Mi"
        cpu: "16"
      limits:
        memory: "128Mi"
        cpu: "16"
